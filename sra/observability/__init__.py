"""Execution Logger: human-readable and structured audit trail of planner
decisions, tool calls, reflections, state transitions, budget, and failures.

Named `observability` (not `logging`) to avoid confusion with the stdlib module.
"""
